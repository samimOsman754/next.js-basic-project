//client Metadata
import Counter from "./Counter";
export const metadata = {
  title: "Counter",
};

const Countpage = () => {
  return (
    <div>
      <Counter />
    </div>
  );
};

export default Countpage;
